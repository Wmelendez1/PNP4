<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="NodeEntryPoint">
          <match url="/*" />
          <!-- Forward all requests to server.js -->
          <action type="Rewrite" url="server.js"/>
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
